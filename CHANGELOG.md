# Changelog

이 파일은 `D:\AI App\monthly` 프로젝트에서 발생하는 모든 변경 사항을 기록하는 데 사용됩니다.

## [2026-09-21]
- **추가**: `CHANGELOG.md` 생성 및 변경 사항 추적 시작.
- **수정**: 홈페이지 렌더링 시 발생하는 자바스크립트 에러 해결.
- **수정**: 총 수입 표시 영역을 '대상별(계원/동욱)'에서 '카테고리별(급여, 보너스 등)'로 변경 (`app.js` 렌더링 로직 수정).
- **수정**: 대시보드의 '총 배분 및 지출' 카드를 화면에서 숨김 처리 (`index.html` 수정).
- **수정**: '가구 총 남은 잔액' 카드의 명칭을 '총 남은 잔액'으로 변경하고, 눈에 덜 띄도록 하이라이트 효과 제거 및 글자 크기 축소 (`index.html` 수정).
- **수정**: '월급 배분 내역'의 총 수입이 잘못 계산되던 버그 수정 (`app.js` 내 중복 함수 제거 및 수입 테이블 금액과 실시간 연동).
- **추가**: '월급 배분 내역' 상단에 '대상 필터' 툴바 추가 및 선택한 대상(계원/동욱/전체)에 맞춰 카드 및 수입/잔액 연동 표시 기능 추가 (`index.html`, `app.js` 수정).
- **수정**: '월급 배분 내역' 테이블의 '일자/이체' 통합 컬럼을 '이체일자'와 '이체방식' 2개의 별도 컬럼으로 분리하여 각 셀 직접 수정 및 추가 모달 연동 (`index.html`, `app.js` 수정).
- **추가**: '월급 배분 내역' 카테고리를 수입 관리처럼 배지 클릭 시 드롭다운 선택 및 사용자 지정 새 카테고리 추가 기능 연동 (`app.js` 수정).
- **추가**: '월급 배분 내역' 상단에 '⚙️ 카테고리 설정' 버튼 추가 및 카테고리 관리 모달에서 월급 배분 카테고리(생활비, 저축/적금, 투자/연금 등) 추가/수정/삭제 종합 관리 기능 구현 (`index.html`, `app.js` 수정).
- **수정**: 인라인 카테고리 선택 드롭다운 이벤트 핸들링(Blur/Change/Event propagation) 강화로 클릭 즉시 동적 선택 및 색상 배지 실시간 반영 로직 보완 (`app.js` 수정).
- **추가**: 대시보드의 '카테고리별 배분 비중' 카드 내 카테고리별 금액, 비중(%), 색상 배지 요약 카드 컴포넌트 추가 및 차트 실시간 동기화 (`index.html`, `app.js` 수정).
- **개선**: 모바일 기기(iOS/Android 스마트폰) 및 웹 환경 호환성을 위해 카테고리 변경 시 인터랙티브 터치 모달 시트(`openCategoryPickerModal`) 적용 (`style.css`, `app.js` 수정).

## [2026-09-22]
- **삭제**: '월급 배분 내역' 상단의 '대상 필터' (전체 보기 / 계원 / 동욱 / 공통 / 가구 / 공용) 툴바 제거 및 계원/동욱 카드 항상 병렬 표시로 변경 (`index.html`, `app.js` 수정).
- **수정**: UI 애니메이션 속도를 대폭 개선하여 (0.3s -> 0.1s) 화면 전환 및 모달 팝업 등 전체적인 렌더링 속도 체감 향상 (`style.css` 수정).
- **버그 수정**: '카테고리 설정' 버튼 클릭 시 모달창이 열리지 않던 문제 해결 (`getAllocCategoryBadgeClass` 함수의 스코프를 전역으로 분리, `app.js` 수정).
- **삭제**: 대시보드의 '월별 수입 및 지출 추이 (Month-over-Month Trend)' 차트 영역 제거 (`index.html` 수정).
- **삭제**: '수입 통합 관리' 영역의 대상 필터 툴바 제거 (`index.html`, `app.js` 수정).
- **개선**: '수입 통합 관리'와 '월급 배분 내역' 영역의 각 '카테고리 설정' 버튼 클릭 시, 해당 영역에 맞는 카테고리만 모달창에 표시되도록 분리하여 UI 혼동 최소화 (`app.js` 수정).
- **삭제**: '수입 통합 관리' 영역 하단의 '수입 요약 배너' (계원 수입, 동욱 수입 등 요약바) 제거 (`index.html`, `app.js` 수정).
- **디자인 수정**: '수입 통합 관리' 표의 입금일(월급날) 및 대시보드 타임라인의 '달력 아이콘' 제거 (`app.js` 수정).
- **구조 변경**: '수입 통합 관리'에서 '수입 카테고리'와 직접 입력하는 '수입 항목명'이 중복되는 문제를 해결하기 위해, 자유 텍스트 입력('수입 항목명')을 없애고 설정된 '수입 카테고리'만 선택하도록 표 및 추가/수정 창 통폐합 (`index.html`, `app.js` 수정).
- **디자인 수정/버그 수정**: 배분 내역 요약바의 '총 수입'과 '배분 합계'에 '원' 글자가 중복 출력되던 현상 수정 (`index.html` 수정) 및 배분 카테고리 종류(저축/적금, 투자/연금 등)별로 다채로운 색상이 적용되도록 CSS 클래스 추가 (`style.css` 수정).
- **편의성 개선**: 월급 배분 내역의 '이체방식' 열을 '자동이체' 전용 체크박스로 변경하여, 클릭 한 번으로 쉽게 자동이체 여부를 켜고 끌 수 있도록 UI 단순화 (`index.html`, `app.js` 수정).
